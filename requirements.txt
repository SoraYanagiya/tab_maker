fastapi>=0.110
uvicorn[standard]>=0.27
music21>=9.1
python-multipart>=0.0.9
httpx>=0.27
pytest>=8.0

# スタンドアロンアプリ（macOS / Windows）用
# pywebview自体は環境マーカー付きの依存関係を内部で持つため、OSに応じて必要なものだけが入る
pywebview>=6.0
pyinstaller>=6.0

# macOS: pywebviewのネイティブウィンドウ表示に必要
pyobjc-framework-Cocoa>=10.0; sys_platform == "darwin"
pyobjc-framework-WebKit>=10.0; sys_platform == "darwin"
pyobjc-framework-Quartz>=10.0; sys_platform == "darwin"

# Windows: pywebviewのEdge WebView2バックエンドに必要
pythonnet>=3.0; sys_platform == "win32"
