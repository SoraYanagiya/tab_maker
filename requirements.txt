fastapi>=0.110
uvicorn[standard]>=0.27
music21>=9.1
python-multipart>=0.0.9
httpx>=0.27
pytest>=8.0

# For the standalone desktop app (macOS / Windows).
# pywebview declares its own platform-specific extras, so only the
# relevant backend gets pulled in per OS.
pywebview>=6.0
pyinstaller>=6.0

# macOS: required for pywebview's native window (Cocoa/WebKit) backend.
pyobjc-framework-Cocoa>=10.0; sys_platform == "darwin"
pyobjc-framework-WebKit>=10.0; sys_platform == "darwin"
pyobjc-framework-Quartz>=10.0; sys_platform == "darwin"

# Windows: required for pywebview's Edge WebView2 backend.
pythonnet>=3.0; sys_platform == "win32"
